# NestJS backend URL (tanda-overtime-backend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Must be IDENTICAL to the backend's JWT_SECRET - the middleware verifies the
# session cookie itself (to redirect to /login) before any page renders.
# Never expose this with a NEXT_PUBLIC_ prefix - it must stay server-only.
JWT_SECRET=
